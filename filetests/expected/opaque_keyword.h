#ifndef __opaque_keyword_h__
#define __opaque_keyword_h__

/**
 * If given, a directly attached documentation is also moved to the header.
 */
struct _Rainbow;
typedef struct _Rainbow Rainbow;
struct _should_also_work;
typedef struct _should_also_work should_also_work;

#endif
